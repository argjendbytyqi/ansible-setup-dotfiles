---
- name: Tools Setup
  hosts: localhost
  become: yes
  vars: 
    ansible_connection: local
  
  roles:
    - orchestration

  tasks:
    - name: Final message
      debug:
        msg: "✅ All tools installed successfully!"
